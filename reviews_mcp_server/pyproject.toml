[project]
name = "reviews-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "aurimasgriciunas", email = "griciunas.aurimas@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.4.4",
    "openai>=2.42.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.1",
    "qdrant-client>=1.18.0",
]

[project.scripts]
reviews-mcp-server = "reviews_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
